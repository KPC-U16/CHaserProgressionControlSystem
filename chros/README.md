# CHroS WebApp
Next.js製のWebアプリケーション。ビューアーとコントローラーを実装しています。

## 環境構築
必要なツール
- Node.js

以下の手順に従って，環境構築を進めてください。
1. `npm install` を実行
2. `.env.example` を `.env` にコピー
3. 環境変数を設定する

## 環境変数
|キー|必須？|内容|
|---|---|---|
|`NEXT_PUBLIC_WEBSOCK_SERVER`|`true`|CHroS WebSock Server のURL|