# 今回の試作: 永続化ディレクトリ。未指定はNext.jsの作業ディレクトリの .chros
# CHROS_DATA_DIR=/absolute/path/to/chros-data

# 将来のPostgreSQL実装用（現在の試作では未使用）
DATABASE_URL=postgresql://user:password@localhost:5432/database

# ViewerのSSE接続先は同一オリジン /api/stream。
